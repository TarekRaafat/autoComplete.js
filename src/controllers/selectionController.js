/**
 * onSelection function
 *
 * @param {*}
 * @param feedback
 *
 * @return void
 */
export default (data, feedback) => {
  // Data feedback function
  // invoked on user selection
  feedback(data);
};
